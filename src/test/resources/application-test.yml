spring:

  flyway:
    enabled: false
    locations: classpath:/db/migration

#fixme: use flyway migrations for tests too
db.hbm2ddl: create

storage:
  type: memory
  #  type: postgres
  algorithm: linked
#  algorithm: skiplist
#  algorithm: withfactor